<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <record id="view_course_list" model="ir.ui.view">
        <field name="name">management.course.list</field>
        <field name="model">management.course</field>
        <field name="arch" type="xml">
            <list>
                <field name="name"/>
                <field name="code"/>
                <field name="credit"/>
            </list>
        </field>
    </record>

    <record id="view_course_form" model="ir.ui.view">
        <field name="name">management.course.form</field>
        <field name="model">management.course</field>
        <field name="arch" type="xml">
            <form>
                <sheet>
                    <group>
                        <group>
                            <field name="name"/>
                            <field name="code"/>
                        </group>
                        <group>
                            <field name="credit"/>
                        </group>
                    </group>
                    <field name="student_ids" widget="many2many_tags"/>
                </sheet>
            </form>
        </field>
    </record>

    <record id="action_course" model="ir.actions.act_window">
        <field name="name">Courses</field>
        <field name="res_model">management.course</field>
        <field name="view_mode">list,form</field>
    </record>

    <menuitem id="menu_courses" name="Courses" parent="menu_student_management" action="action_course" sequence="30"/>
</odoo>